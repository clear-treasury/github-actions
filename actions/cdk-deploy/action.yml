name: CDK deploy
description: deploy some cdk
inputs:
  environment-name:
    required: true
  aws-access-key-id:
    required: true
  aws-secret-access-key:
    required: true
  aws-region:
    required: true
  working-directory:
    required: false
    default: ./
branding:
  icon: upload-cloud
  color: orange
runs:
  using: composite
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
        cache: npm
    - name: install dependancies
      working-directory: ${{ inputs.working-directory }}
      run: npm ci
      shell: bash
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ inputs.aws-access-key-id }}
        aws-secret-access-key: ${{ inputs.aws-secret-access-key }}
        aws-region: ${{ inputs.aws-region }}
    - name: CDK bootstrap
      working-directory: ${{ inputs.working-directory }}
      run: node_modules/.bin/cdk bootstrap
      shell: bash
    - name: CDK deploy
      working-directory: ${{ inputs.working-directory }}
      run: >
        npm exec cdk deploy --require-approval=never -c branch="${{ inputs.environment-name }}"
        -c stack_name="${{ inputs.environment-name }}-${{ github.event.repository.id }}"
        -c stack_description="${{ github.event.repository.html_url }}/tree/${{ inputs.environment-name }}"
        --all
      shell: bash
